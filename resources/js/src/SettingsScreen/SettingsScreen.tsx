import React from 'react';
import Header from '../shared/Header';

const SettingsScreen = () => {
    return (
        <div className="SettingsScreen__container">
            <Header />
            This is the SettingsScreen component.
        </div>
    );
};

export default SettingsScreen;